#include "main.h"

/**
 * _abs - gives you the absolute value of a number
 *
 * @n: The number to get its absolute value
 *
 * Return: the absolute value of the number
 */

int _abs(int n)
{
	if (n < 0)
	{
		return  (-n);
	}
	else
	{
		return (n);
	}
}
